from fastapi import APIRouter

router = APIRouter()

# TODO: Phase 7 — Quiz generation endpoints
# POST /api/quizzes/generate      → generate quiz from summary
# GET  /api/quizzes/{video_id}    → get quiz for a video
# POST /api/quizzes/{id}/submit   → submit quiz answers + return score
