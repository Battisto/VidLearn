from fastapi import APIRouter

router = APIRouter()

# TODO: Phase 6 — Summarization endpoints
# GET  /api/summaries/{video_id}  → get summary for a video
# POST /api/summaries/generate    → trigger summarization job
